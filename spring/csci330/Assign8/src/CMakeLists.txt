
add_executable(z2004200 z2004200.cxx) # Exe (main main.cpp)

# add_library() # Adds Lib (hello hello.cpp)

# target_link_libraries() # Links src files (main hello)

# configure_file(log ${CMAKE_CURRENT_BINARY_DIR}/log COPYONLY) # for input files needed at runtime.
